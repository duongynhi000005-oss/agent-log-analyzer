# Paid Scan Receipt Email

Use this copy when receipt/support email delivery is implemented. Keep the language aligned with the website, report, downloaded report, and plugin artifact.

## Subject

Your Claude Analyzer report and benchmark-backed optimization pack are ready

## Body

Your paid Claude Analyzer scan is complete.

The report was generated locally from your recent Claude Code sessions. Raw transcripts stayed on your machine; only the sanitized aggregate report was uploaded.

What your download includes:

- a short-lived sanitized report URL
- a generated Claude Code optimization plugin
- workflow guidance for scoped reads, output-budgeted commands, retry breaks, and session hygiene
- conditional third-party reducer recommendations only when your report shows the matching waste pattern

Why we are confident:

- our repeated Agent Analyzer benchmark ran three fresh Claude Code pairs from the same commit and prompt
- both baseline and optimized runs had to pass the same `go test ./...` quality gate
- the optimized runs averaged `12,370` fewer estimated tokens and `12,698` fewer tool-output tokens
- repriced at published Claude Sonnet 4.6 API rates, the workflow reduced cost by `23.986%`

At comparable scale, `23.986%` is about `$1,199/month` on `$5,000/month` of Claude Sonnet API-equivalent coding usage. Basis: the benchmark moved from `$0.2468368` baseline to `$0.1876295` optimized, a `-$0.0592073` mean delta. Your actual savings depend on whether your future work has a similar token mix and quality requirements.

What we do not claim:

- ccusage and ccstatusline are measurement tools, not token reducers
- claude-context, Probe, Caveman for Claude, claude-rlm, and claude-token-efficient are not default reducer recommendations in this pack
- output-token savings are not the same as reasoning-token or full-session cost savings

Open your report first. Then install the plugin only after reviewing the waiver and the matching findings it is designed to address.

Support: reply to this email with your report ID and the benchmark artifact link shown in the report.
